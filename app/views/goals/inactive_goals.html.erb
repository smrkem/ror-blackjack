<h1>Previous Goals</h1>

<ul id="my_goals_list">
  <% if @goals.empty? %>
    <li>You have no previous goals.</li>
  <% else %>
    <% @goals.each do |goal| %>
      <%= render partial: "inactive_goal", locals: { goal: goal } %>
    <% end %>
  <% end %>
</ul>
